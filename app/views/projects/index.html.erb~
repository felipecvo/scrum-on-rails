
<% content_for :header do -%>
  Lista de Projetos
<% end -%>


<table>
  <tr>
    <th>Name</th>
    <th>Uri</th>
  </tr>

<% for project in @projects %>
  <tr>
    <td><%=h project.name %></td>
    <td><%=h project.uri %></td>
    <td><%= link_to 'detalhar', project %></td>
    <td><%= link_to 'editar', edit_project_path(project) %></td>
    <td><%= link_to 'usuários', :controller => "project_users", :action => "edit", :pid => project.id %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'novo projeto', new_project_path %>
