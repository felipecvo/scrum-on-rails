<% content_for :header do -%>
  New task
<% end -%>

<% form_for([@project, @story, @task]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :user_id %><br />
    <%= collection_select(:task, :user_id, @users, :id, :login) %>
  </p>
  <p>
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </p>
  <p>
    <%= f.submit "Criar" %>
  </p>
<% end %>

<%= link_to 'voltar', project_story_tasks_path %>
